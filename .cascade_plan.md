# Plan

1. Lift the three.js background component from example.js into the app (as a reusable lib component).
2. Mount it in the root layout behind all content with safe sizing/layering and SSR guards.
3. Verify it tracks DaisyUI theme changes (data-theme / custom vars) and tweak styles if needed.
