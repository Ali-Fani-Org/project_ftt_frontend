# Plan
1) Inspect custom theme source (theme.js) and current custom theme handling in stores/layout/settings.
2) Add the hardcoded theme (dark-futuristic) to available themes and ensure theme-change sees it.
3) Wire UI (settings/select) to include it and confirm apply logic (built-in vs custom).
