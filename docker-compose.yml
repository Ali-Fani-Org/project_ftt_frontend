services:
  web:
    build: .
    ports:
      - "3000:80"  # Map external 3000 to internal 80
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost/healthz > /dev/null || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped